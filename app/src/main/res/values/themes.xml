<resources>
    <!-- Base application theme. -->
    <style name="Theme.MovieRate" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
        <item name="black">@color/black</item>
        <item name="progressBackground">@dimen/progressBackground</item>
    </style>
    <style name="NavigationTheme" parent="Theme.MaterialComponents.Light">
        <item name="android:backgroundTint">@color/colorPrimary</item>
    </style>
</resources>